VITE_API_BASE_URL=http://localhost:5000/api
VITE_WS_URL=http://localhost:5000
# Public member site — super.* root redirects here (required in production, e.g. https://www.yoursite.com).
# VITE_MAIN_SITE_URL=http://localhost:5173
# Set true only when testing staff UI on localhost without super.* DNS (disables public routes on that dev server).
# If true, set VITE_MAIN_SITE_URL to your real public URL so / can redirect off the staff-only dev server.
# VITE_FORCE_STAFF_PORTAL=false
