/**
 * OneSignal browser SDK — init after user consent and when env is configured.
 */
export function initOneSignal(_appId: string | undefined) {
  if (!_appId) return
  // TODO: import OneSignal and initialize
}
